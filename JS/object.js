/*  Тask_1 
let user = {
  name: "John",
  surname: "Smith",
}
delete user.name; */


/*Тask_2
 const user = {
    name: "John"
}
user.name ="pete"; 
Содержимое объекта менять можно,так как user хранит ссылку на объект и она остается не изменной а данные внутри менять можно*/


/*Тask_3
 let sum;
let salaries = {
    John: 100,
    Ann: 160,
    Pete: 130
  };
sum=salaries.John+salaries.Ann+salaries.Pete;
alert(sum); */
 
/* Task_4
1. Boolean(Number(’10’)) + 1=2
Number(’10’)-преобразовывает строку в число
Boolean-Преобразовывает число в true/fals, так как число больше 0 оно становится true=1. тогда 1+1=2

2.‘sub ’ + Number(false)= sub0 строка+0

3.  16  *  ‘91‘=1552 - так как умножение это только численый метод стока преобразовывается в число 

4. true-71= -70 true=1

5. Number(1 + String(1)) + 1=12 
(1 + String(1)- сначала происходит конкотенация строк 1(число)+1(строка)=11 происходит схлопывание зачем преобразовывается все в число
 */
